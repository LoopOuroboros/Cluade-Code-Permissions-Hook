{
  "hooks": {
    "SessionStart": [
      {
        "matcher": "startup|resume|clear|compact",
        "hooks": [
          {
            "type": "command",
            "command": "node \"${CLAUDE_PLUGIN_ROOT}/scripts/session-start.js\""
          }
        ]
      }
    ],
    "TaskCreated": [
      {
        "hooks": [
          {
            "type": "prompt",
            "prompt": "Output ONLY the following JSON object. Do not add any other text before or after it:\n\n{}\n\n--- Context ---\nTask created: $ARGUMENTS. Good practice: keep at most 3 tasks InProgress simultaneously. When all plan tasks are done, verify with TaskList then clean up with TaskUpdate."
          }
        ]
      }
    ],
    "TaskCompleted": [
      {
        "hooks": [
          {
            "type": "prompt",
            "prompt": "Output ONLY the following JSON object. Do not add any other text before or after it:\n\n{}\n\n--- Context ---\nTask completed: $ARGUMENTS. Good practice: check remaining tasks with TaskList. When all plan tasks are completed, confirm with TaskList then clean up with TaskUpdate."
          }
        ]
      }
    ],
    "Stop": [
      {
        "hooks": [
          {
            "type": "command",
            "command": "node \"${CLAUDE_PLUGIN_ROOT}/scripts/stop-cleanup.js\""
          }
        ]
      }
    ]
  }
}
